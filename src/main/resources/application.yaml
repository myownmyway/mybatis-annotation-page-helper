server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/generator?serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/**.xml
  type-aliases-package: com.wpw.mybatisannotationpagehelper.pojo
  configuration:
    map-underscore-to-camel-case: on
pagehelper:
  reasonable: true